<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @category.name %>
</p>
Products:
  <% @category.products.each do |product| %>
    <%# <%= link_to product.user.username, user_path(product.user) %> said %>
    <%= product.name %>
  <% end %>

<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>
